// The S1 test site's addresses. A module of its own with no side effects, so importing the
// address doesn't drag the scenario catalog into a build.
export const SITE = 'http://localhost:8765';
export const OTHER_ORIGIN = 'http://127.0.0.1:8765';
